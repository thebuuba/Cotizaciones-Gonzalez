import { CheckCircle2, ChevronLeft, FileImage, FileText, Loader2, MapPin, Maximize2, Minus, MoreHorizontal, Pencil, Phone, Plus, Share2, Trash2, X } from 'lucide-react'
import { useEffect, useRef, useState, type TouchEvent as ReactTouchEvent } from 'react'
import { createPortal } from 'react-dom'
import { Link } from 'react-router-dom'

import { calculateMaterialTotal, calculateQuotationTotals, formatMoney } from '../../domain/money'
import type { QuotationSnapshot, QuotationStatus } from '../../domain/types'
import { QuotationDocument } from '../export/QuotationDocument'
import { createExportDiagnostics } from '../export/exportDiagnostics'
import { exportQuotationImages, exportQuotationPdf, shareOrDownload } from '../export/exportService'

const statusLabels: Record<QuotationStatus, string> = { draft: 'Borrador', sent: 'Enviada', approved: 'Aprobada', rejected: 'Rechazada' }
function formatDate(value: string): string { const [year, month, day] = value.split('-'); return year && month && day ? `${day}/${month}/${year}` : value }
function formatQuantity(quantityMilli: number): string { return new Intl.NumberFormat('es-DO', { maximumFractionDigits: 3 }).format(quantityMilli / 1000) }
const nextPaint = () => new Promise<void>((resolve) => requestAnimationFrame(() => resolve()))
async function letLoadingScreenPaint(): Promise<void> { await nextPaint(); await nextPaint() }

export function buildQuotationShareText(snapshot: QuotationSnapshot): string { const totals = calculateQuotationTotals(snapshot.materialItems, snapshot.quotation.laborMinor); const materials = snapshot.materialItems.length ? snapshot.materialItems.map((item, index) => `${index + 1}. ${item.description} — ${formatQuantity(item.quantityMilli)} ${item.unit} × ${formatMoney(item.unitPriceMinor)} = ${formatMoney(calculateMaterialTotal(item))}`) : ['Sin materiales registrados.']; return [snapshot.business.businessName, `Cotización ${snapshot.quotation.number}`, `Cliente: ${snapshot.quotation.clientName}`, snapshot.quotation.clientAddress ? `Dirección: ${snapshot.quotation.clientAddress}` : '', `Fecha: ${formatDate(snapshot.quotation.issueDate)}`, '', 'Materiales:', ...materials, '', `Total de materiales: ${formatMoney(totals.materialsMinor)}`, `Mano de obra: ${formatMoney(totals.laborMinor)}`, `Total general: ${formatMoney(totals.totalMinor)}`, snapshot.quotation.observations ? `\nObservaciones: ${snapshot.quotation.observations}` : ''].filter((line) => line !== '').join('\n') }

export function QuotationDetailPage({ snapshot, onStatusChange, onDelete }: { snapshot: QuotationSnapshot; onStatusChange: (status: QuotationStatus) => void | Promise<void>; onDelete: () => void | Promise<void> }) {
  const exportDocumentRef = useRef<HTMLDivElement>(null); const [exporting, setExporting] = useState<'pdf' | 'image' | 'share' | null>(null); const [exportReady, setExportReady] = useState(false); const [exportMessage, setExportMessage] = useState(''); const [menuOpen, setMenuOpen] = useState(false); const [previewOpen, setPreviewOpen] = useState(false); const [previewScale, setPreviewScale] = useState(1); const menuRef = useRef<HTMLDivElement>(null); const previewPinchRef = useRef<{ distance: number; scale: number } | null>(null); const totals = calculateQuotationTotals(snapshot.materialItems, snapshot.quotation.laborMinor); const baseName = `${snapshot.quotation.number} ${snapshot.quotation.clientName}`; const exportPages = () => Array.from(exportDocumentRef.current?.querySelectorAll<HTMLElement>('[data-export-page]') ?? []); const showReadyState = async () => { setExportReady(true); await nextPaint() }; const beginExport = async (type: 'pdf' | 'image' | 'share') => { setExportReady(false); setExporting(type); setExportMessage(''); await letLoadingScreenPaint() }
  const shareQuotation = async () => { await beginExport('share'); const text = buildQuotationShareText(snapshot); try { if ('share' in navigator && typeof navigator.share === 'function') { await showReadyState(); await navigator.share({ title: `Cotización ${snapshot.quotation.number}`, text }); setExportMessage('Cotización compartida') } else if (navigator.clipboard?.writeText) { await navigator.clipboard.writeText(text); setExportMessage('Resumen copiado al portapapeles') } else throw new Error('Compartir no disponible') } catch (error) { if (!(error instanceof DOMException && error.name === 'AbortError')) { console.error('Error al compartir cotización', error); setExportMessage('No se pudo compartir la cotización. Intenta nuevamente.') } } finally { setExportReady(false); setExporting(null) } }
  const exportPdf = async () => { await beginExport('pdf'); const pages = exportPages(); const diagnostics = createExportDiagnostics('pdf', snapshot.quotation.id, pages.length); await diagnostics.start(); try { const file = await exportQuotationPdf(pages, baseName, diagnostics.progress); const result = await shareOrDownload([file], showReadyState, diagnostics.progress); await diagnostics.success({ bytes: file.size, result }); setExportMessage('PDF preparado') } catch (error) { await diagnostics.error(error); console.error('Error al exportar PDF', error); setExportMessage('No se pudo exportar el PDF. Intenta nuevamente.') } finally { setExportReady(false); setExporting(null) } }
  const exportImages = async () => { await beginExport('image'); const pages = exportPages(); const diagnostics = createExportDiagnostics('image', snapshot.quotation.id, pages.length); await diagnostics.start(); try { const files = await exportQuotationImages(pages, baseName, diagnostics.progress); const result = await shareOrDownload(files, showReadyState, diagnostics.progress); await diagnostics.success({ files: files.length, bytes: files.reduce((sum, file) => sum + file.size, 0), result }); setExportMessage('Imagen preparada') } catch (error) { await diagnostics.error(error); console.error('Error al exportar imagen', error); setExportMessage('No se pudo exportar la imagen. Intenta nuevamente.') } finally { setExportReady(false); setExporting(null) } }
  useEffect(() => { if (!menuOpen) return; const handleClick = (event: MouseEvent) => { if (menuRef.current && !menuRef.current.contains(event.target as Node)) setMenuOpen(false) }; document.addEventListener('mousedown', handleClick); return () => document.removeEventListener('mousedown', handleClick) }, [menuOpen]); useEffect(() => { if (!exporting && !previewOpen) return; const previousOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previousOverflow } }, [exporting, previewOpen]); useEffect(() => { if (!previewOpen) { setPreviewScale(1); previewPinchRef.current = null } }, [previewOpen])
  const handlePreviewTouchStart = (event: ReactTouchEvent<HTMLDivElement>) => { if (event.touches.length !== 2) { previewPinchRef.current = null; return }; const [first, second] = [event.touches[0], event.touches[1]]; previewPinchRef.current = { distance: Math.hypot(second.clientX - first.clientX, second.clientY - first.clientY), scale: previewScale } }; const handlePreviewTouchMove = (event: ReactTouchEvent<HTMLDivElement>) => { if (event.touches.length !== 2 || !previewPinchRef.current) return; if (event.cancelable) event.preventDefault(); const [first, second] = [event.touches[0], event.touches[1]]; const distance = Math.hypot(second.clientX - first.clientX, second.clientY - first.clientY); setPreviewScale(Math.min(3, Math.max(.75, previewPinchRef.current.scale * (distance / Math.max(1, previewPinchRef.current.distance))))) }; const handlePreviewTouchEnd = (event: ReactTouchEvent<HTMLDivElement>) => { if (event.touches.length < 2) previewPinchRef.current = null }; const handleDelete = async () => { setMenuOpen(false); if (window.confirm('¿Seguro que deseas eliminar esta cotización?')) await onDelete() }
  const loadingCopy = exportReady ? ['Listo para compartir', 'Selecciona dónde deseas enviar o guardar el archivo.'] : exporting === 'pdf' ? ['Preparando PDF…', 'Generando el documento.'] : exporting === 'image' ? ['Preparando imagen…', 'Renderizando la cotización.'] : ['Abriendo opciones…', 'Preparando la cotización para compartir.']; const exportOverlay = exporting ? createPortal(<div className={`quotation-export-loading${exportReady ? ' is-ready' : ''}`} role="status" aria-live="assertive"><div className="quotation-export-loading__card">{exportReady ? <CheckCircle2 className="quotation-export-loading__check" aria-hidden="true" /> : <Loader2 className="quotation-export-loading__spinner" aria-hidden="true" />}<strong>{loadingCopy[0]}</strong><span>{loadingCopy[1]}</span></div></div>, document.body) : null
  const previewViewer = previewOpen ? createPortal(<div className="quotation-preview-viewer" role="dialog" aria-modal="true" aria-label="Vista ampliada de la cotización"><header className="quotation-preview-viewer__toolbar"><button type="button" onClick={() => setPreviewOpen(false)} aria-label="Cerrar vista previa"><X aria-hidden="true" /></button><strong>Vista previa</strong><div className="quotation-preview-viewer__zoom" aria-label="Controles de zoom"><button type="button" onClick={() => setPreviewScale((value) => Math.max(.75, value - .25))} disabled={previewScale <= .75} aria-label="Alejar"><Minus aria-hidden="true" /></button><span>{Math.round(previewScale * 100)}%</span><button type="button" onClick={() => setPreviewScale((value) => Math.min(3, value + .25))} disabled={previewScale >= 3} aria-label="Acercar"><Plus aria-hidden="true" /></button></div></header><div className="quotation-preview-viewer__scroll" onTouchStart={handlePreviewTouchStart} onTouchMove={handlePreviewTouchMove} onTouchEnd={handlePreviewTouchEnd} onTouchCancel={handlePreviewTouchEnd}><div className="quotation-preview-viewer__stage" style={{ width: `${100 * previewScale}%` }}><div className="quotation-preview-viewer__document" style={{ transform: `scale(${previewScale})` }} onDoubleClick={() => setPreviewScale((value) => value > 1 ? 1 : 2)}><QuotationDocument snapshot={snapshot} /></div></div></div></div>, document.body) : null
  return <div className="quotation-detail quotation-detail-ios" aria-busy={Boolean(exporting)}>{exportOverlay}{previewViewer}<nav className="quotation-detail-nav" aria-label="Navegación de la cotización"><Link className="quotation-detail-nav-back" to="/cotizaciones" aria-label="Volver a cotizaciones"><ChevronLeft aria-hidden="true" />Cotizaciones</Link><strong>Cotización</strong><div className="actions-menu" ref={menuRef}><button className="quotation-detail-menu-button" type="button" onClick={() => setMenuOpen((open) => !open)} aria-label="Acciones de la cotización"><MoreHorizontal aria-hidden="true" /></button>{menuOpen && <div className="actions-menu__dropdown quotation-detail-menu"><Link className="actions-menu__item" to={`/cotizaciones/${snapshot.quotation.id}/editar`} onClick={() => setMenuOpen(false)}><Pencil aria-hidden="true" />Editar</Link>{snapshot.client.phone && <a className="actions-menu__item" href={`tel:${snapshot.client.phone}`} onClick={() => setMenuOpen(false)}><Phone aria-hidden="true" />Llamar</a>}<button className="actions-menu__item actions-menu__item--danger" type="button" onClick={() => void handleDelete()}><Trash2 aria-hidden="true" />Eliminar</button></div>}</div></nav><header className="quotation-detail-hero"><span>{snapshot.quotation.number}</span><h1>{snapshot.quotation.clientName}</h1><p><MapPin aria-hidden="true" />{snapshot.quotation.clientAddress || 'Sin dirección'}</p><strong>{formatMoney(totals.totalMinor)}</strong><label className={`quotation-status-control quotation-status-control--${snapshot.quotation.status}`}><span className="sr-only">Estado de la cotización</span><select value={snapshot.quotation.status} onChange={(event) => void onStatusChange(event.target.value as QuotationStatus)} aria-label="Estado de la cotización">{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label></header><section className="quotation-detail-group" aria-labelledby="quotation-summary-title"><h2 id="quotation-summary-title">Resumen</h2><div className="quotation-detail-card"><div className="quotation-detail-row"><span>Fecha</span><strong>{formatDate(snapshot.quotation.issueDate)}</strong></div><div className="quotation-detail-row"><span>Materiales</span><strong>{formatMoney(totals.materialsMinor)}</strong></div><div className="quotation-detail-row"><span>Mano de obra</span><strong>{formatMoney(totals.laborMinor)}</strong></div><div className="quotation-detail-row quotation-detail-row--total"><span>Total general</span><strong>{formatMoney(totals.totalMinor)}</strong></div></div></section><section className="quotation-detail-group" aria-labelledby="quotation-materials-title"><h2 id="quotation-materials-title">Materiales</h2><div className="quotation-detail-card quotation-material-list">{snapshot.materialItems.map((item) => <div className="quotation-material-row" key={item.id}><div><strong>{item.description}</strong><small>{formatQuantity(item.quantityMilli)} {item.unit} × {formatMoney(item.unitPriceMinor)}</small></div><strong>{formatMoney(calculateMaterialTotal(item))}</strong></div>)}{!snapshot.materialItems.length && <div className="quotation-material-empty">Sin materiales registrados.</div>}</div></section>{snapshot.quotation.observations && <section className="quotation-detail-group" aria-labelledby="quotation-observations-title"><h2 id="quotation-observations-title">Observaciones</h2><div className="quotation-detail-card quotation-detail-note">{snapshot.quotation.observations}</div></section>}<section className="quotation-detail-group" aria-labelledby="quotation-export-title"><h2 id="quotation-export-title">Compartir</h2><div className="quotation-detail-card quotation-detail-actions"><button className="quotation-detail-action" type="button" disabled={Boolean(exporting)} onClick={() => void shareQuotation()}><span className="quotation-detail-action-icon"><Share2 aria-hidden="true" /></span><span><strong>{exporting === 'share' ? 'Abriendo opciones…' : 'Compartir cotización'}</strong><small>Enviar un resumen como texto por WhatsApp, Mensajes o correo</small></span></button><button className="quotation-detail-action" type="button" disabled={Boolean(exporting)} onClick={() => void exportPdf()}><span className="quotation-detail-action-icon"><FileText aria-hidden="true" /></span><span><strong>{exporting === 'pdf' ? 'Creando PDF…' : 'Exportar PDF'}</strong><small>Documento listo para enviar o imprimir</small></span></button><button className="quotation-detail-action" type="button" disabled={Boolean(exporting)} onClick={() => void exportImages()}><span className="quotation-detail-action-icon"><FileImage aria-hidden="true" /></span><span><strong>{exporting === 'image' ? 'Creando imagen…' : 'Exportar imagen'}</strong><small>Imagen lista para compartir</small></span></button><button className="quotation-detail-action" type="button" disabled={Boolean(exporting)} onClick={() => setPreviewOpen(true)}><span className="quotation-detail-action-icon"><Maximize2 aria-hidden="true" /></span><span><strong>Vista previa</strong><small>Ver el documento antes de exportar</small></span></button></div>{exportMessage && <p className="quotation-export-message" role="status">{exportMessage}</p>}</section><div className="quotation-export-stage" aria-hidden="true"><div ref={exportDocumentRef}><QuotationDocument snapshot={snapshot} /></div></div></div>
}
